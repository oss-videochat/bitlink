{
  "name": "@bitlink/bitlink",
  "version": "0.3.0",
  "private": true,
  "scripts": {
    "setup": "lerna bootstrap",
    "start": "lerna run --stream start"
  },
  "dependencies": {
    "lerna": "^3.22.1",
    "morgan": "^1.10.0"
  }
}
